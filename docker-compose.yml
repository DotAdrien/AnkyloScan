services:
  frontend:
    build: ./frontend
    ports:
      - "8000:80"

api:
    build: ./api
    ports:
      - "8001:8001"
    env_file:
      - .env  # Ajoute cette ligne pour que l'API voit le MDP 🔑
    volumes:
      - ./api:/app

  db:
    build: ./mysql
    environment:
      MYSQL_DATABASE: ankyloscan
      MYSQL_USER: admin # Ton nouvel utilisateur 👤
      MYSQL_PASSWORD: ${ADMIN_PASSWORD:-password_aleatoire} # Mot de passe variable 🔑
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - db_data:/var/lib/mysql

  welcome:
    image: alpine
    depends_on:
      - frontend
    command: >
      sh -c "
      echo '--------------------------------------------' &&
      echo '🦖 AnkyloScan est prêt ! 🛡️' &&
      echo 'Lien : http://localhost:8000' &&
      echo 'User Admin : admin' &&
      echo 'MDP Admin : ${ADMIN_PASSWORD:-password_aleatoire}' &&
      echo 'Tigrounet veille sur tes données ! 🐱😶' &&
      echo '--------------------------------------------'"

volumes:
  db_data:
