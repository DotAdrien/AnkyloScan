services:
  frontend:
    build: ./frontend
    ports:
      - "8000:80"

  api:
    build: ./api
    ports:
      - "8001:8001"
    volumes:
      - ./api:/app

  db:
    build: ./mysql
    ports:
      - "8002:3306"
    environment:
      MYSQL_DATABASE: ankyloscan
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - db_data:/var/lib/mysql

  # ✨ Ce bloc affiche tes messages dans le terminal
  welcome:
    image: alpine
    depends_on:
      - frontend
    command: >
      sh -c "echo '--------------------------------------------' &&
             echo '🦖 AnkyloScan est prêt ! 🛡️' &&
             echo 'Lien : http://localhost:8000' &&
             echo 'Tigrounet veille sur tes données ! 🐱😶' &&
             echo '--------------------------------------------'"

volumes:
  db_data:
