[package]
name = "scanner"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
mysql = "24.0.0" # Pour que Tigrounet puisse enregistrer les résultats 🐱